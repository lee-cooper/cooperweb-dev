// How to use console Log

// Logging objects in console log

// logging with a message and an object

// console.log('This can be anything', { hello: 'Greetings' }, 'hi');
const person = {
  name: 'Bob',
};

console.log(person);

person.name = 'Jim';

console.log(person);
